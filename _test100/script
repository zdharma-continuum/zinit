tst_verbosity

zinit ice depth"1"
zinit load psprint/zsh-select
zinit load zdharma/ice-order

print -rl -- "${(o)ZINIT_ICE[@]}" >! answer/ice
print -rl -- "${(okv)ZINIT_SICE[@]}" >! answer/sice

zinit list > answer/loaded
zinit compiled > answer/compiled

local -a lines
lines=( "${(f)"$( zinit zstatus )"}" )
lines=( "${lines[@]/*\/test/}" )
print -rl -- "${lines[@]}" > answer/zstatus

zinit csearch > answer/csearch

# vim:ft=zsh
